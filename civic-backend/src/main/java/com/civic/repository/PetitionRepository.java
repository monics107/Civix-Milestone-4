package com.civic.repository; import com.civic.entity.*; import com.civic.enums.PetitionStatus; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*;
public interface PetitionRepository extends JpaRepository<Petition,Long>{Page<Petition> findByStatus(PetitionStatus s,Pageable p); Page<Petition> findByCategory(String c,Pageable p); Page<Petition> findByCreator(User u,Pageable p); Page<Petition> findByCategoryAndStatus(String c,PetitionStatus s,Pageable p); Page<Petition> findByDepartmentIgnoreCase(String d,Pageable p); Page<Petition> findByDepartmentIgnoreCaseAndStatus(String d,PetitionStatus s,Pageable p);}
